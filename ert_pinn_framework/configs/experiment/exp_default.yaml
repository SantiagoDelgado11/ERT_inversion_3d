experiment:
  name: exp_default
  mode: train_then_invert
  save_predictions: true
  save_checkpoints: true

  notes: |
    Default experiment for 3D ERT PINN framework.
    Supports forward training and inverse conductivity estimation.
